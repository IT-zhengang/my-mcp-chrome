# Chrome Extension manifest public key
# Optional override for the checked-in stable key in chrome-extension-public-key.txt
# Use the base64-encoded public key from the extension manifest "key" field, not the 32-char extension ID
CHROME_EXTENSION_PUBLIC_KEY=YOUR_MANIFEST_PUBLIC_KEY_HERE
